feat: offline transaction queue (FR-OFF-2/3/4/6, minimal-correct)

PRD promised offline logging with a local queue, auto-sync on reconnect, a UI
indicator and IndexedDB persistence (FR-OFF-2/3/4/6, all P0/P1). Nothing
existed — ARCHITECTURE referenced lib/offline/queue.ts that was never written.

Built the minimal-but-correct slice the user approved (LWW excluded):

  lib/offline/queue.ts       raw IndexedDB (no wrapper lib): one store, ops
                             keyed by id, survives reload (FR-OFF-6)
  lib/offline/sync.ts        'use client' module; drains oldest-first by
                             calling the REAL createTransaction action.
                             Terminal refusals (validation, insufficient
                             balance, archived wallet) are dropped so the
                             queue cannot wedge; transient failures break and
                             keep the op for the next reconnect (FR-OFF-3)
  components/OfflineIndicator banner + auto-drain on 'online' + on mount;
                             hidden on auth screens
  components/QuickLogSheet   when navigator.onLine is false the submit
                             enqueues instead of erroring — and confirms to
                             the user, because a queued transaction is still a
                             recorded transaction, just delivered later

Deliberately NOT built (FR-OFF-5): dedupe / last-write-wins. The server action
is not idempotent, so a sync that dies after server commit but before queue
removal can double-create on the next attempt. Marked ponytail: in code with
the clientId-column upgrade path, and in the PRD row.

The test suite (15 checks) is logic + structural: IndexedDB does not exist in
Node and importing QuickLogSheet pulls Next's react/loader, so the queue CRUD,
the drain ordering/failure contract ('use client' boundary included — that one
caught a comment-vs-code false negative) and the two submit branches are
asserted by source inspection. A real browser offline E2E remains the gap.

289 -> 304 checks green across 29 suites.