test: verify dashboard math against PRD 6.7, add server preflight

New test (7 checks) recomputes every headline number straight from the
database and asserts the rendered page agrees, rather than trusting that
a plausible-looking number is correct:

  - Total Saldo == sum of non-archived wallet balances
  - Aman Harian == max(0, floor((liquid - vaults - debts) / daysLeft))
  - Month-to-date Masuk/Keluar with transfers EXCLUDED
  - A 999000 transfer provably does not inflate Keluar

The daysLeft term is recomputed per run, so the test stays correct as the
calendar rolls over (it moved from 21 to 20 days mid-session and still passed).

Two things worth recording about getting here:

1. A confusing "flaky" failure was self-inflicted: the runner loop invoked
   scripts/test-dashboard.js while the file is test-dashboard-math.js, so
   Node reported MODULE_NOT_FOUND. It only ever failed inside the loop,
   which made it look like shared state or connection exhaustion. It was a
   filename mismatch in my own command.

2. The currency parser initially read every digit in a region and produced
   garbage (1250000 -> -141250000112624). Now it matches a single "Rp ..."
   token.

Also adds a server preflight: a dead server used to surface as a curl/JSON
error deep in the run, looking like a test bug. Now it fails fast naming
the real cause.

163 checks green across 16 suites.