feat: close three feature gaps — Quick-Log ergonomics + partial debt payment

Audited the 80 FR rows against the code and found three P0/P1 promises that
had no implementation:

1. FR-LOG-2 (P0): amount field now autoFocus on open.
2. FR-LOG-7 (P0): wallet select defaults to last-used wallet (localStorage
   key kasdesk:last-wallet), fallback to the first wallet; choice persists
   across opens.
3. FR-DBT-3 (P0): partial debt payment. settleDebt(id, amount?) —
   undefined keeps the old full-settle behaviour, a number adds a partial
   payment to paidAmount. Refuses non-positive amounts and overpay (no silent
   clamping — "paid 20k" must not silently become 15k). isPaid flips only
   when paidAmount reaches amount; settledAt is set only on completion and
   preserved for still-open debts. UI: "Bayar Sebagian" modal per open debt
   row (Wallet icon, remaining shown, numeric input).

Notes on the JSX surgery: the partial-pay modal inserted cleanly, but the
first edit dropped the opening fragment <> for the button cluster and the
state additions were written in a second pass — tsc caught both (JSX parent
error) before anything ran. The settleDebt select also had to gain settledAt
so the update can preserve it.

test:debt-partial (13/13) checks the action contract structurally, replays the
action's SQL sequence for a 60k + 40k split on a 100k debt (isPaid stays 0
until the total), and confirms ownership scoping is intact. Old test:debts
still 15/15 (backward compatible).

304 -> 317 checks green across 30 suites.