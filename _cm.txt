feat: execute PRD — real-data UI, missing routes, isolation tests

Replaces the hard-coded homepage mockup with live TiDB data and
implements the screens the PRD specifies but that did not exist.

Data-layer wiring
- app/page.tsx now reads getDashboard/getRecentTransactions/getWallets
  instead of hard-coded Rp 14.250.000 and fake rows
- lib/actions.ts: add getSpendingFlow, getTopCategories, createWallet;
  getDebts now ordered (unpaid first, soonest due)
- WalletSchema type enum corrected to match stored values (e_wallet,
  investment) — 'e-wallet' would have been rejected

New screens (previously 404 from BottomNav)
- app/wallets/page.tsx + components/WalletsClient.tsx (list, total,
  working "Dompet Baru" sheet — the + button was previously inert)
- app/wallets/[id]/page.tsx (per-wallet balance + grouped history)
- app/vaults/page.tsx, app/insights/page.tsx (7-day flow, top
  categories, debts)

Components
- components/AppShell.tsx: renders BottomNav only on authed routes
  (was never mounted anywhere before)
- components/BottomNav.tsx: active-link state via usePathname
- components/QuickLogSheet.tsx: FAB now opens a real expense form
  wired to createTransaction
- components/EmptyState.tsx, lib/format.ts (IDR/date helpers)

Tests (previously zero)
- scripts/test-isolation.js — 10 checks proving user B gets 404 on
  user A's wallet. This is the RLS replacement; MySQL has no RLS so
  the userId filter is the only boundary.
- scripts/smoke-ui.js — 19 checks, every route renders real data
- scripts/seed-demo.js — idempotent demo data
- package.json: seed:demo, test:smoke, test:isolation

Results: tsc clean, build green, 9 routes, smoke 19/19, isolation 10/10.
