type Level='info'|'warn'|'error';type Fields=Record<string,string|number|boolean|null|undefined>;const BLOCKED=/password|secret|token|authorization|cookie|image|receipt/i;export function log(level:Level,event:string,fields:Fields={}){const safe=Object.fromEntries(Object.entries(fields).filter(([k,v])=>!BLOCKED.test(k)&&v!==undefined));const record=JSON.stringify({level,event,at:new Date().toISOString(),...safe});if(level==='error')console.error(record);else if(level==='warn')console.warn(record);else console.info(record)}
