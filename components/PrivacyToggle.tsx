'use client';import {useEffect,useState} from 'react';import {Eye,EyeOff} from 'lucide-react';export function PrivacyToggle(){const [hidden,setHidden]=useState(false);useEffect(()=>{const v=localStorage.getItem('kasdesk:privacy')==='hidden';document.documentElement.classList.toggle('privacy-mode',v);requestAnimationFrame(()=>setHidden(v))},[]);function toggle(){const n=!hidden;setHidden(n);document.documentElement.classList.toggle('privacy-mode',n);localStorage.setItem('kasdesk:privacy',n?'hidden':'shown')}return <button type="button" onClick={toggle} aria-label={hidden?'Tampilkan nominal':'Sembunyikan nominal'} className="grid h-11 w-11 place-items-center rounded-xl text-text-secondary">{hidden?<EyeOff className="h-5 w-5"/>:<Eye className="h-5 w-5"/>}</button>}
