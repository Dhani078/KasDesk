'use client';import {useEffect,useState} from 'react';import {Moon,Sun} from 'lucide-react';export function ThemeToggle(){const [theme,setTheme]=useState<'dark'|'light'>('dark');useEffect(()=>{const v=(localStorage.getItem('kasdesk:theme') as 'dark'|'light'|null)??(matchMedia('(prefers-color-scheme: light)').matches?'light':'dark');document.documentElement.dataset.theme=v;requestAnimationFrame(()=>setTheme(v))},[]);function toggle(){const n=theme==='dark'?'light':'dark';setTheme(n);document.documentElement.dataset.theme=n;localStorage.setItem('kasdesk:theme',n)}return <button type="button" onClick={toggle} aria-label="Ganti tema" className="grid h-11 w-11 place-items-center rounded-xl border border-border-outer bg-surface">{theme==='dark'?<Sun className="h-5 w-5"/>:<Moon className="h-5 w-5"/>}</button>}
