feat: KASDESK baseline - PRD, specs, and TiDB Cloud backend

Migrates the app from Supabase to TiDB Cloud (MySQL-compatible) and adds
the full specification set that was previously missing.

Product and docs
- Add PRD.md (80 requirements: FR/NFR, personas, metrics, risks)
- Add ARCHITECTURE.md (16 MADR ADRs, C4 diagrams, routing)
- Add DATABASE-SPEC.md, SECURITY-SPEC.md, DESIGN-SYSTEM.md,
  AI-OCR-SPEC.md, QA-STRATEGY.md
- Add conductor/product.md (context-driven-development mirror)
- Mark 9 legacy docs as superseded by PRD.md

Backend: Supabase -> TiDB Cloud
- Add Drizzle schema for users/accounts/sessions/categories/wallets/
  transactions/vaults/debts plus migration SQL and scripts/migrate.js
- Add lib/db connection pool with TLS and hot-reload reuse
- Remove lib/supabase and the @supabase/* dependencies
- Rewrite lib/actions.ts: createTransaction now updates wallet balance
  atomically inside one SQL transaction (fixes balances never updating)
- lib/schemas.ts: amount is now .int().positive() (rejects 0 and negatives)
- Rename useVaultifyStore -> useKasdeskStore, add optimistic rollback

Chores
- Ignore generated PWA output and local agent artifacts
- Keep .env.example tracked while ignoring .env* secrets
